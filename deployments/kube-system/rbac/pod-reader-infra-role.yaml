kind: Role
apiVersion: rbac.authorization.k8s.io/v1beta1
metadata:
  namespace: infra
  name: pod-reader
rules:
- apiGroups: [""] # "" indicates the core API group
  resources: ["pods"]
  verbs: ["get", "list"]
